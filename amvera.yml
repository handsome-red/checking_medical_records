meta:
  environment: docker
  version: latest

build:
  dockerfile: Dockerfile
  context: .

run:
  containerPort: 8080
  command: ./server

variables:
  DB_HOST: localhost
  DB_PORT: "5432"
  DB_USER: atlas
  DB_PASSWORD: "123"
  DB_NAME: med_book